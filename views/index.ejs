<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@300;400;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="style.css">
    <title>Anime Voice</title>
</head>
<% if (locals.content) { %>
<body>
  <div class="frame">
    <div class="card" role="region" aria-label="Quote card">
      <div class="card-inner">

        <div class="content">
          <div class="quote-mark">“</div>
            
          <div class="quote-text"><%= content.data.content %></div>

          <div class="decor" aria-hidden="true">
            <div class="dot"></div>
            <div class="line"></div>
            <div class="dot"></div>
          </div>

          <div class="meta"><%= content.data.character.name %></div>
          <div class="submeta"><%= content.data.anime.name %></div>

        </div>

      </div>
    </div>
  </div>

  <script>
    function randomColor(){
      return `hsl(${Math.floor(Math.random()*360)}, 75%, 65%)`;
    }
    document.documentElement.style.setProperty('--card-bg', randomColor());
  </script>
  
  <% } %>
</body>
</html>